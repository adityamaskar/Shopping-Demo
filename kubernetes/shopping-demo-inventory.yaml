apiVersion: apps/v1
kind: Deployment
metadata:
  name: shopping-demo-inventory
spec:
  replicas: 3
  selector:
    matchLabels:
      app: shopping-demo-inventory
  template:
    metadata:
      labels:
        app: shopping-demo-inventory
    spec:
      initContainers:
        - name: wait-for-mysql
          image: busybox:1.28
          command:
            - 'sh'
            - '-c'
            - 'until nc -z mysql-service 3306; do echo "Waiting for MySQL..."; sleep 2; done; echo "MySQL is up!"'
      containers:
        - name: shopping-demo-inventory
          image: adityamaskar1/shopping-demo-inventory:1.0
          imagePullPolicy: Always
          ports:
            - containerPort: 8080
          env:
            - name: spring.datasource.password
              valueFrom:
                secretKeyRef:
                  name: app-secrets
                  key: mysql-root-password
            - name: spring.datasource.url
              valueFrom:
                configMapKeyRef:
                  name: shopping-config
                  key: DB_URL_INVENTORY
            - name: spring.kafka.bootstrap-servers
              valueFrom:
                configMapKeyRef:
                  name: shopping-config
                  key: KAFKA_BROKERS
            - name: server.port
              value: "8080"
---
apiVersion: v1
kind: Service
metadata:
  name: shopping-demo-inventory-service
spec:
  selector:
    app: shopping-demo-inventory
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
#      nodePort: 32000
  type: ClusterIP